# ACR Mobile CDS Parity — What, Why and How v1.6

**Status:** Instructions prepared; Mac implementation/builds not executed here.
**Baseline:** ACR Companion v0.5.1 Build 43, preserved. **Next target:** v0.6.0 Build 44, subject to verified current metadata.
**Authority:** Kraken controls scope, mandatory handovers, acceptance and release. This pair supersedes v1.0–v1.5.

## What and why

Build the mock server and functional API interface for all 20 assessment values across Step 1, Step 2, Step 3, P1 and P2. Mobile connects through the SAME verified back-office route as the existing website. Ontolator — © BlockEnergy / CRIL — remains the platform's connected Ontology/SWRL reasoning component. Platform Openllet, Java fallback, custom SWRL, aggregation and optional Bayes are not duplicated in mobile or gateway.

Identical canonical patient facts, Bayes setting and backend/assets/evaluation context must yield the same substantive clinical result and explanation. The mock replays only verified platform-generated synthetic fixtures, explicitly labelled as no current inference. Unreachable services produce errors, never invented clinical results. P1/P2 transport proceeds with source-backed provisional mappings.

## Exact action sequence

| Step | Codex action and required human handover |
| --- | --- |
| 2.1 | Read both current instructions fully; preserve Build 43 source/artefacts; verify established build conditions and Mac no-sleep arrangement. |
| 2.2 | Examine current web/API mapping and identify both local services, established startup procedure and actual reasoner route. Reuse healthy services; Kraken starts missing back-office services. |
| 2.3 | Implement mock/gateway and mobile connection; reaffirm backend flow read-only; test and repair with independent review. If services are off, source/local work continues; live checks stay explicitly pending. |
| 2.4 first | Build/install/launch the EXISTING iPhone 16e simulator. STOP for Kraken's offline/mock and online inspection. Ask Kraken to start the two services for online checks; complete all required live/fixture evidence and obtain explicit simulator approval. |
| 2.4 next | Only then request the unlocked iPhone 13/iOS 26.6.1. Verify it, stop simulator/Metro, build signed standalone Release using existing automatic provisioning renewal, install Build 44 and check launch. |
| 2.5 | Ask Kraken to unplug iPhone 13 and confirm standalone relaunch. WAIT for confirmation; then record iOS stage complete. |
| 2.6 | Only now request the USB3 drive at /Volumes/AndroidDev AND the unlocked, already set-up Samsung S8. Verify actual mount and USB authorisation before Android work. |
| 2.7 | Build/audit/install/launch Android Build 44 on the S8 using the established external SDK/cache/ADB and low-memory Gradle command. NO Android emulator. |
| 2.8 | Produce final evidence and post-build clinician companion; complete bounded housekeeping; invite Kraken's final inspection of BOTH physical Build 44 apps. |
| 2.9 throughout | A separate reviewer cross-checks each stage against actual code/tests/artefacts. Verified defects, corrections and reusable checks pass forward in the saved journal. |

“Offline mock” means the back-office reasoner is unavailable but the Mac mock/gateway remains reachable. Replay is an explicitly chosen synthetic demonstration, not phone-side reasoning. A fully disconnected phone shows unavailable. If no verified fixture exists, Codex must disclose pending capture, obtain it when services are available, then complete mock review before physical iOS. Already-running services are reused; unavailable behaviour may be tested through a clearly labelled task-owned simulation, removed before real online review. Codex never stops or requests shutdown of shared services to manufacture an offline test.

## Operation and protection

Use actual parent/child agents and a distinct reviewer, at most one source writer and one heavy build at a time. Three bounded repair rounds and saved checkpoints support evidence-led improvement and resumption. A reviewer PASS does not replace Kraken's simulator approval or device handovers. Keep the simulator, Metro and gateway available during simulator review; no advancing on silence.

The Mac stays on AC power with its lid open and a verified no-sleep arrangement. Codex may maintain a tracked temporary keep-awake assertion without changing persistent power/security settings. At human gates it saves state, asks once and yields. An overnight run therefore pauses for simulator review or an earlier genuine dependency; it does not automatically install both phones.

Preserve Build 43's recoverable source and existing artefacts before Build 44 edits/output replacement. Build 44 uses a separate development branch and replaces installed Build 43 only at the authorised device stages. Preserve native projects, installed mobile dependencies, automatic signing design, external SDK/cache/ADB and internal APK output. No Expo Prebuild, Android emulator, toolchain upgrade, broad cleanup, canonical clinical-source modification, VPS deployment, commit, push or distribution.

## Files and final return

Keep the current v1.6 pair directly under:

```text
/Users/Kraken/DAPP/acr-mobile-companion-extended/docs/
```

The existing `docs/archive/` holds older reference documents; do not execute them, reorganise them or require the never-downloaded v1.2.

Generated outputs also go directly under `docs/`: backend-flow reaffirmation, post-build clinician functional-flow companion, execution evidence and resumable state/journal. They are outputs, not missing prerequisites. Final evidence records both physical builds, hashes, human gates, independent checks, residual issues and proposed commits. Final readiness invites Kraken's review; it does not claim acceptance or authorise release.

Use the complete Loop's Section 19 opening statement in local Codex.
