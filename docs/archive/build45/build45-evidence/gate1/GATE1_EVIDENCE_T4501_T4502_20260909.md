# Build 45 — Gate 1 Evidence: T45-01 and T45-02

**Session:** ACR-Mobile-Build45-Loop-v1.0-S001
**Authority:** `ACR_MOBILE_BUILD45_AUTONOMOUS_EXECUTION_LOOP_v1_1.md` §3 (Gate 1)
**Date:** 9 September 2026
**Executor:** Claude Code
**Isolated worktree:** `/Users/Kraken/DAPP/REVIEW-WORKTREES/acr-platform-build45-evidence` (detached at `33daead3`)
**Result: GATE 1 PASS** — both blockers closed. Canonical checkout provably unchanged.

All input is synthetic. No patient data, name, hospital identifier or contact detail
is present in any run, log, fixture or artefact.

---

## 1. Checkpoints

| Step | Result |
|---|---|
| G1-01 | PASS — detached worktree created at pinned SHA `33daead3`. |
| G1-02 | PASS — canonical tracked diff **empty**, staged diff **empty**, HEAD `33daead3` before any work. |
| G1-03 | PASS — T45-01 executed, all differences enumerated (§3). |
| G1-04 | PASS — T45-02 executed; fail-closed proven at the gateway (§4). |
| G1-05 | PASS — one test-only addition, **zero** main-source modification (§5). |
| G1-06 | PASS — canonical status and diff **byte-identical** to the G1-02 baseline. |
| G1-07 | PASS — worktree retained intact, not deleted. |

## 2. Method

`ReasonerService` uses constructor injection, so both deterministic paths and the
forced Bayesian failure were produced **without modifying any production source**.
The harness (`Gate1EvidenceT4501T4502Test.java.txt`, test-only, in the worktree only)
constructs `ReasonerService` with real collaborators, varying one at a time:

- **Path A — Openllet/SWRL:** the real, fully wired `OntologyLoader`.
- **Path B — Java hardcoded fallback:** a Mockito spy over that same real loader whose
  `getSwrlRuleCountAfterInjection()` returns 0. This is the **production selection
  predicate** in `performOWLSWRLReasoningWithMode` — no production logic is bypassed,
  reimplemented, or short-circuited; the real `executeHardcodedReasoning` runs.
- **T45-02 fault:** a `BayesianEnhancer` subclass whose `enhance()` genuinely throws.

**Fixtures — two variants.** The stored `e2e/fixtures/luminal-b-request.json` carries
only **14 of the 21** assessment properties defined by
`schemas/acr.cds.v1.request.schema.json`; it omits `tumorSize`, `gender`, `ecogScore`,
`pdl1Status`, `her2Low`, `lvef` and `treatmentIntent`, which caps it at data-completeness
Tier 1. Running only that fixture would not satisfy "the complete synthetic fixture",
so T45-01 was executed against **both**:

- `storedFixture14of21` — the stored fixture exactly as committed (Tier 1, blocked);
- `completeFixture21of21` — the same case with all seven omitted fields supplied with
  synthetic, schema-valid, clinically coherent values (Tier 3, full assessment).

Four comparison runs in total (2 fixtures × Bayes ON/OFF).

Runtime attestation observed: ontology `ACR_Ontology_Full_v2_2.owl`,
SWRL load mode `EMBEDDED_ONLY`, **76 SWRL axioms after injection**, 71 logical /
76 physical / 76 active — consistent with the Build 44 preserved attestation.

Build: `mvn -o test` under JDK 21.0.11, offline. **Tests run: 2, Failures: 0, Errors: 0.**

---

## 3. T45-01 — Openllet/SWRL vs Java fallback: complete enumeration

**Exactly 5 differences in every one of the four runs** — identical across both fixture
variants and under Bayes ON and Bayes OFF. Raw evidence:
`T45-01-dual-path-comparison.json`.

| Run | Difference count | Differing fields |
|---|---|---|
| `storedFixture14of21` + Bayes ON | 5 | the five below |
| `storedFixture14of21` + Bayes OFF | 5 | the five below |
| `completeFixture21of21` + Bayes ON | 5 | the five below |
| `completeFixture21of21` + Bayes OFF | 5 | the five below |

The conclusion therefore holds for both a Tier-1 blocked assessment and a Tier-3 full
assessment, not only for the incomplete stored fixture.

### 3.1 Differences (all provenance/explanation metadata)

| # | Field | Openllet/SWRL | Java fallback |
|---|---|---|---|
| 1 | `data.reasoningMode` | `OPENLLET_SWRL` | `JAVA_HARDCODED_FALLBACK` |
| 2 | `deterministic.reasoningMode` | `OPENLLET_SWRL` | `JAVA_HARDCODED_FALLBACK` |
| 3 | `reasoning.firedRules[]` | 5 entries — includes `R2/R3 \| Luminal B HER2-negative classification \| OPENLLET_SWRL \| FIRED` | 4 entries — **no classification rule at all** |
| 4 | `reasoning.rulesFired[0]` | `"SWRL R2/R3: Luminal B HER2-negative classification"` | `"SWRL R1-R6: Classification rules evaluated (no match)"` |
| 5 | `reasoning.trace` | "…determined via Openllet SWRL engine… load mode: EMBEDDED_ONLY; runtime SWRL axiom count after merge: 76…" | "…determined via Java hardcoded fallback reasoner… Openllet SWRL engine inactive…" |

### 3.2 Fields that are IDENTICAL across both paths

**No clinical divergence whatsoever, in either fixture variant.** Every clinically
meaningful output is identical between the two reasoners:

| Field | `storedFixture14of21` (both paths) | `completeFixture21of21` (both paths) |
|---|---|---|
| `deterministic.molecularSubtype` | `LuminalB_HER2Negative` | `LuminalB_HER2Negative` |
| `deterministic.riskLevel` | `LOW` | `INTERMEDIATE` |
| `data.riskLevel` (top level) | `null` (suppressed, tier ≤ 2) | `INTERMEDIATE` |
| `deterministic.treatments` | 2 entries (chemo+endocrine; R33 follow-up cadence) | 3 entries (adds `"Adjuvant chemotherapy + endocrine therapy (R17b — Luminal B T2 post-surgery)"`) |
| `deterministic.biomarkers` | `{ER, PR, HER2, Ki67, Stage}` | adds `HER2-low: positive`, `PD-L1: negative` |
| `bayesian.enabled` | `true` | `true` |
| `bayesian.confidence` | `0.6001915864330829` | `0.6001915864330829` (**unchanged**) |
| `bayesian.posterior` | `{Luminal_A: 0.60019…, Luminal_B: 0.36278…, Normal_Like: 0.03507…, HER2_Enriched: 0.00140…, Triple_Negative: 0.00055…}` | identical |
| `bayesian.uncertaintyBounds` | `[0.47393…, 0.72645…]` | identical |
| `reasoning.evidence` | 5 lines | 9 lines (adds PD-L1, HER2-low, ECOG, LVEF) |
| `dataCompleteness.tier` | `1` | `3` |
| `dataCompleteness.missingFields` | `["tumorSize"]` | `[]` |
| `dataCompleteness.rulesBlocked` | `64` | `0` |
| `dataCompleteness.warning` | `"❌ ASSESSMENT BLOCKED — Essential biomarker data missing or invalid: tumorSize"` | `"✅ FULL ASSESSMENT — All data complete."` |

**T45-01 conclusion:** across both fixture variants and both Bayes settings, the two
reasoners agree completely on subtype, risk, treatments, biomarkers, completeness and
Bayes. They differ only in how they
*describe* the derivation. The `reasoningMode` provenance label is truthful and
correctly distinguishes the paths.

### 3.3 Observations arising (carried forward, not closed here)

- **OBS-1 — misleading fallback rule text.** On the fallback path `rulesFired[0]` reads
  `"SWRL R1-R6: Classification rules evaluated (no match)"`, yet the fallback **did**
  determine `LuminalB_HER2Negative`. The text implies no classification occurred.
- **OBS-2 — fallback `firedRules[]` omits the classification rule.** The structured
  array contains only the four ACR_NATIVE rules, so a consumer reading `firedRules[]`
  cannot tell what produced the subtype on the fallback path. → Gate 7 / **T45-04**.
- **OBS-3 — `firedRules[]` order is non-deterministic.** Across the eight recorded runs
  the four ACR_NATIVE rules `{R33, RE3, RE4-1, RE4-2}` appear in **eight different
  orderings**:

  | Run | Path | Order |
  |---|---|---|
  | stored, Bayes ON | A | `R2/R3, R33, RE4-1, RE3, RE4-2` |
  | stored, Bayes ON | B | `R33, RE4-2, RE4-1, RE3` |
  | stored, Bayes OFF | A | `R2/R3, RE3, R33, RE4-1, RE4-2` |
  | stored, Bayes OFF | B | `RE3, R33, RE4-1, RE4-2` |
  | complete, Bayes ON | A | `R2/R3, RE3, RE4-2, RE4-1, R33` |
  | complete, Bayes ON | B | `R33, RE4-1, RE3, RE4-2` |
  | complete, Bayes OFF | A | `R2/R3, R33, RE4-1, RE4-2, RE3` |
  | complete, Bayes OFF | B | `RE4-2, RE4-1, R33, RE3` |

  The classification rule `R2/R3` is deterministically first whenever present (it is
  explicitly prepended in code); the remainder derive from unordered iteration over the
  engine's audit list. Ordering is therefore **not** currently a carrier of meaning, and
  any consumer that treats `firedRules[0..n]` as ranked or chronological would be
  reading noise. → **direct empirical input to T45-04 at Gate 7.**
- **OBS-4 — a blocked assessment still carries `deterministic.riskLevel = LOW`.**
  In the `storedFixture14of21` (Tier 1) runs, top-level `riskLevel` is correctly
  suppressed to `null`, but the nested `deterministic.riskLevel` still reads `LOW`
  beneath an "❌ ASSESSMENT BLOCKED" warning. A consumer reading the nested field
  instead of the top-level one would show a reassuring LOW risk for an assessment the
  platform considers unusable. The Tier-3 run does not exhibit this (both fields read
  `INTERMEDIATE`). → Gate 7 and clinical question **C45-03**.
- **OBS-5 (CORRECTED) — the stored e2e fixture is incomplete against the governed
  contract.** An earlier draft of this document stated that `tumorSize` "is not among
  the mobile fields". **That was wrong.** `tumorSize` *is* one of the 21 governed
  assessment properties in `schemas/acr.cds.v1.request.schema.json`, and
  `gateway/src/mapper.js` transports all 21. The real finding is that the stored
  fixture `e2e/fixtures/luminal-b-request.json` supplies only **14 of 21** properties —
  omitting `tumorSize`, `gender`, `ecogScore`, `pdl1Status`, `her2Low`, `lvef` and
  `treatmentIntent` — which is why it alone produces a Tier-1 blocked, 64-rules-blocked
  assessment. Supplying all 21 moves the same case to Tier 3, 0 rules blocked,
  `INTERMEDIATE` risk and an additional R17b treatment. Two consequences:
  1. Any Phase-G or parity work driven solely by this fixture exercises two-thirds of
     the governed contract and never reaches the full-assessment path. → **T45-06**,
     Gate 11.
  2. The fixture is labelled `"captureRoute": "MOCK_AUTHORED_BUILD_43"` and
     `"eligibleForDeliveredReplay": false`, so this is a known-mock limitation rather
     than a regression — but it should be replaced by a complete captured fixture
     before Gate 11. → Gate 6 / **T45-05** and **C45-02**.

---

## 4. T45-02 — Forced `BayesianEnhancer` exception

Raw evidence: `T45-02-forced-bayes-failure.json`, `T45-02-wire-failed.json`,
`T45-02-wire-healthy.json`.

### 4.1 Platform behaviour — does NOT represent the failure correctly

`ReasonerService.performInference` catches the exception internally (the catch is
commented "Return partial result with error handling"), so `InferenceController`'s own
catch never fires and the platform returns **HTTP 200**. The exact wire payload:

```json
{
  "success" : true,
  "message" : "Inference completed successfully",
  "data" : {
    "patientId" : "mob-3f9c1e84-77a2-4b16-9d05-2ce8a1b47f30",
    "deterministic" : { "molecularSubtype": null, "riskLevel": null,
                        "treatments": null, "biomarkers": null, "reasoningMode": null },
    "bayesian" : { "confidence": 0.0, "posterior": {},
                   "uncertaintyBounds": [0.0, 0.0], "enabled": false },
    "reasoning" : null,
    "riskLevel" : null,
    "dataCompleteness" : null,
    "reasoningMode" : "OPENLLET_SWRL",
    "molecularSubtype" : null
  },
  "apiVersion" : "2.0"
}
```

| Observation | Result |
|---|---|
| Exception propagated to caller | **No** — swallowed |
| HTTP status | **200 OK** |
| `success` flag | **`true`** |
| `message` | **"Inference completed successfully"** |
| Clinical output retained or invented | **No** — subtype, risk, treatments, biomarkers, reasoning, dataCompleteness all `null` |
| `reasoningMode` after failure | **`OPENLLET_SWRL` — stale and misleading** |
| `bayesian.enabled` after failure | `false` — **indistinguishable from a legitimate Bayes-OFF run** |

**Finding — PLATFORM DOES NOT FAIL CORRECTLY.** No clinical value is invented, which is
the critical safety property. But the response asserts success, carries no error
signal, and retains a `reasoningMode` claiming live Openllet reasoning for an inference
that did not complete. Any consumer trusting `success`/`reasoningMode` without
validating the payload body would misread a failure as a live result.

### 4.2 Gateway behaviour — FAILS CLOSED (verified)

The exact serialised payloads above were driven through the gateway's real guard
(`gateway/src/upstream-validator.js` → `validatePlatformSuccess`):

```
--- HEALTHY platform response (T45-02-wire-healthy.json)
    ACCEPTED by gateway    resultMode: LIVE_REASONER
--- BAYES-FAILURE platform response (T45-02-wire-failed.json)
    REJECTED — FAIL CLOSED
    code=INVALID_UPSTREAM_RESPONSE  http=502  outcome=FAILED
    message="The reasoner returned an invalid success response (deterministic result)."
```

**T45-02 conclusion:** the gateway path **fails closed**. A `BayesianEnhancer`
exception cannot deliver partial or invented clinical output to the phone — the
guard rejects the payload at the `deterministic result` check before any mapping,
and returns a `FAILED` outcome rather than a degraded result. The platform's own
failure representation is nonetheless defective and is recorded below.

### 4.3 Observations arising

- **OBS-6 — platform reports success on failure.** `success: true` +
  "Inference completed successfully" + HTTP 200 for a failed inference. The gateway
  compensates, but the platform contract is untruthful. Platform-side remediation is
  **out of scope for Build 45** (canonical checkout is read-only; cf. T45-03 treatment)
  — recorded for the platform backlog.
- **OBS-7 — stale `reasoningMode` survives the failure.** Set at STEP 2, never cleared
  by the catch block. Directly contradicts truthful-delivery labelling.
- **OBS-8 — gateway rejection is incidental, not intentional.** The guard rejects on
  `deterministic result` (null subtype), not on an explicit failure signal, because the
  platform sends none. The fail-closed outcome is correct but rests on payload-shape
  validation. Recommend Gate 7/Gate 10 add an explicit assertion that a `null`
  `deterministic.molecularSubtype` with a non-null `reasoningMode` is treated as
  `FAILED` by contract, so the behaviour is guaranteed rather than emergent.

---

## 5. G1-05 — Test-only changes inside the isolated worktree

| Change | Rationale |
|---|---|
| **Added** `ACR-Ontology-Interface/src/test/java/org/acr/platform/build45/Gate1EvidenceT4501T4502Test.java` | The only source addition. Drives both reasoning paths and the forced Bayes fault through the production constructor and production selection predicate, and serialises the true wire payloads for the gateway fail-closed check. |

**Main-source modifications: ZERO** (`git diff -- '*/src/main/*'` → 0 lines).
No fault injection was added to production code; the fault is a test-local
`BayesianEnhancer` subclass. `target/` contains build output only.

## 6. G1-06 — Canonical checkout unchanged

| Check | Baseline (G1-02) | After Gate 1 (G1-06) |
|---|---|---|
| HEAD | `33daead3223605a44c4b1175bd656d2a5039bfa6` | identical |
| Tracked diff | empty (0 lines) | empty (0 lines) |
| `status --porcelain` | 2 pre-existing untracked docs | **byte-identical** |
| Full `git diff` output | 0 bytes | **byte-identical** |

The two untracked files under `docs/Mobile_App/` were present at session start and are
unchanged. All canonical-checkout access this session was read-only.

## 7. G1-07 — Evidence retained

The worktree is retained intact for Kraken's review and was **not** deleted:

```
/Users/Kraken/DAPP/ACR-platform                                   33daead [main]
/Users/Kraken/DAPP/REVIEW-WORKTREES/acr-platform-build45-evidence 33daead (detached HEAD)
/Users/Kraken/DAPP/REVIEW-WORKTREES/acr-platform-cb5aff3a         cb5aff3 (detached HEAD)
```

---

## 8. Gate 1 PASS criteria

| Criterion | Status |
|---|---|
| T45-01 comparison fully enumerated and recorded | **MET** — 5 differences in each of 4 runs (stored 14-of-21 and complete 21-of-21 fixtures, Bayes ON/OFF), complete field-by-field record |
| T45-02 fail-closed behaviour proven | **MET** — gateway rejects with 502 `INVALID_UPSTREAM_RESPONSE`, outcome `FAILED` |
| Canonical checkout provably unchanged | **MET** — byte-identical to baseline |
| No STOP condition triggered | **MET** — no non-empty canonical diff; worktree never exposed through any tunnel; no partial/invented clinical output survived to the client |

**T45-01 and T45-02 are closed.** Eight observations (OBS-1 – OBS-8) are carried
forward to Gates 6, 7 and 10 and to the platform backlog; none blocks Gate 1 closure.

---

**END OF GATE 1 EVIDENCE**
